package com.vytrack.tests;

public class VyTrack_Ahmet {
    //first line
}

/*
 1.GITHUB | clone --> copy link from github and paste into the intelliJ after opening File>New>Project from
 version control
 2.INTELLIJ | git branch
 3.INTELLIJ | git checkout -b VyTrack_Ahmet (creates a branch as local repo)
 4 INTELLIJ | git push --set-upstream origin VyTrack_Ahmet (pushes whatever we have to the remote branch)
 5.GITHUB | click "new pull request" then compare changes between master and VyTrack_Ahmet then click
 "Create pull request"
 6.INTELLIJ | git checkout master
 7.INTELLIJ | git pull

Note: When we are in local master branch it's possible to push the changes in the local branch
to the remote local branch ----> git push -u origin VyTrack_Ahmet

 "compare and pull request" then click "pull request" then click"
 */
